To setup dev environment:

```
python -m venv .venv
source .venv/bin/activate
pip install hatchling
pip install -e .
```

Note: The dataset has not yet been cleaned to comply with WOTC's OpenGL policy and as such the backing db must be manually configured at this time.

This project was created as a tool to assist in DMing for D&D 5e and made utilizing Claude Code.
As of June, 4th this repository has been made public as a reference for recruiters.
